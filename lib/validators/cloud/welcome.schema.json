{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "http://apexslicer.com/schemas/cloud/welcome.json",
  "title": "welcome",
  "description": "Abort the current print on the specified printer",
  "type": "object",
  "properties": {
    "challenge": {
      "description": "String which the printer must sign with it's private key and return with a hello command",
      "type": "string",
      "minLength": 1,
      "maxLength": 256
    }
  },
  "required": [
    "challenge"
  ]
}
